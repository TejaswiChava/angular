import { CampusListSearchPipe } from './campus-list-search.pipe';

describe('CampusListSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CampusListSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
