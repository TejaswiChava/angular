import { CampusSearchFilterPipe } from './campus-search-filter.pipe';

describe('CampusSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CampusSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
