import { CampusDrivePipe } from './campus-drive.pipe';

describe('CampusDrivePipe', () => {
  it('create an instance', () => {
    const pipe = new CampusDrivePipe();
    expect(pipe).toBeTruthy();
  });
});
