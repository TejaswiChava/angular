import { CampusEventListPipe } from './campus-event-list.pipe';

describe('CampusEventListPipe', () => {
  it('create an instance', () => {
    const pipe = new CampusEventListPipe();
    expect(pipe).toBeTruthy();
  });
});
