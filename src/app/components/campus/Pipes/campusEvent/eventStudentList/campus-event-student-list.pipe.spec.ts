import { CampusEventStudentListPipe } from './campus-event-student-list.pipe';

describe('CampusEventStudentListPipe', () => {
  it('create an instance', () => {
    const pipe = new CampusEventStudentListPipe();
    expect(pipe).toBeTruthy();
  });
});
