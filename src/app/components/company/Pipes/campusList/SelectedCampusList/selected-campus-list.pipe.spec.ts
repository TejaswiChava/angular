import { SelectedCampusListPipe } from './selected-campus-list.pipe';

describe('SelectedCampusListPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedCampusListPipe();
    expect(pipe).toBeTruthy();
  });
});
