import { GetCollegeListPipe } from './get-college-list.pipe';

describe('GetCollegeListPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCollegeListPipe();
    expect(pipe).toBeTruthy();
  });
});
