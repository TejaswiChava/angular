import { CompanyUploadPipe } from './company-upload.pipe';

describe('CompanyUploadPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyUploadPipe();
    expect(pipe).toBeTruthy();
  });
});
