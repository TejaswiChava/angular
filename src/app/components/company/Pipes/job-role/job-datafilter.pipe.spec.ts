import { JobDatafilterPipe } from './job-datafilter.pipe';

describe('JobDatafilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JobDatafilterPipe();
    expect(pipe).toBeTruthy();
  });
});
