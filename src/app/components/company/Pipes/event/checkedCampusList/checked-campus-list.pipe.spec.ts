import { CheckedCampusListPipe } from './checked-campus-list.pipe';

describe('CheckedCampusListPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckedCampusListPipe();
    expect(pipe).toBeTruthy();
  });
});
