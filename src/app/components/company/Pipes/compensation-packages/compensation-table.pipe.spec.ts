import { CompensationTablePipe } from './compensation-table.pipe';

describe('CompensationTablePipe', () => {
  it('create an instance', () => {
    const pipe = new CompensationTablePipe();
    expect(pipe).toBeTruthy();
  });
});
