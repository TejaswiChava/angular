import { EventStudentListPipe } from './event-student-list.pipe';

describe('EventStudentListPipe', () => {
  it('create an instance', () => {
    const pipe = new EventStudentListPipe();
    expect(pipe).toBeTruthy();
  });
});
