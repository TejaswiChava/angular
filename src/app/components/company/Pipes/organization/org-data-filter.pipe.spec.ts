import { OrgDataFilterPipe } from './org-data-filter.pipe';

describe('OrgDataFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrgDataFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
